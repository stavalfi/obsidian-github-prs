export enum Properties {
	ORG = "org",
	REPO = "repo",
	COLUMNS = "columns",
	AUTHOR = "author",
	STATE = "state",
}

export enum State {
	open = "open",
	closed = "closed",
}

export enum Column {
	REPOSITORY = "Repository",
	BRANCH = "Branch",
	TITLE = "Title",
	STATUS = "Status",
	CREATED = "Created",
	LAST_COMMIT = "Last Commit",
}
